{
    "provider": "openai_chat_completion_client",
    "config": {
        "model": "${LITELLM_MODEL}",
        "api_base": "${LITELLM_API_BASE}",
        "api_key": "${LITELLM_API_KEY}",
        "temperature": 0.7,
        "max_tokens": 512
    }
}