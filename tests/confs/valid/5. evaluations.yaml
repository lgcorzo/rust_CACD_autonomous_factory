job:
  KIND: EvaluationsJob
  inputs:
    KIND: ParquetReader
    path: "${tests_path:}/data/inputs_sample.parquet"
    limit: 4
  targets:
    KIND: ParquetReader
    path: "${tests_path:}/data/targets_sample.parquet"
    limit: 4
